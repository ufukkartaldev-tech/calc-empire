'use client';

import React from 'react';
import type { CalculatorVisualProps } from '@/types';

export function StressStrainVisualizer(_props: CalculatorVisualProps) {
  return <div className="text-6xl">💥</div>;
}
